import React from 'react';
import css from './gray-backdrop.module.scss'
function GrayBackdrop(props) {
    return (
        <div className={css.grayBackdrop}>

        </div>
    );
}

export default GrayBackdrop;
